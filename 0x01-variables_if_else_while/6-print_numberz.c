#include <stdio.h>

/**
 * main - Program to print single digits
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int y;

	for (y = 0; y < 10; y++)
	{
		putchar(y);
	}
	putchar('\n');

	return (0);
}
